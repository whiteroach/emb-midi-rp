# emb-midi-rp

```
cargo build // install dependencies and build 
cd /home/psim/Documents/midi-rp/emb-midi-rp && cargo build --release

probe-rs run --chip RP2040 target/thumbv6m-none-eabi/release/emb-midi-rp

```